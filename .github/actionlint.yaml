paths:
  .github/workflows/copilot-cli-org-billing-test.yml:
    ignore:
      # actionlint v1.7.12 does not yet recognize the billing probe's scope.
      - '^unknown permission scope "copilot-requests"\.'
