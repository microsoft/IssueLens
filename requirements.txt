github-copilot-sdk>=1.0.7

# Both protocol packages plug into the same azure-ai-agentserver-core host:
# invocations (automation) + responses (chat).
azure-ai-agentserver-invocations==1.0.0b7
azure-ai-agentserver-responses==1.0.0b9
azure-identity>=1.17.0
azure-keyvault-secrets>=4.9.0
python-dotenv==1.2.2
PyJWT[crypto]>=2.9.0

# httpx — POST triage notifications to the email / Teams Logic App endpoints.
httpx>=0.27.0

# debugpy enables local debugging of this agent with the Foundry Toolkit VS Code extension.
debugpy
